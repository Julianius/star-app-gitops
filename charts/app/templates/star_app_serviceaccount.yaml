apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "star-app.serviceAccountName" . }}
  labels:
    {{- include "star-app.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}